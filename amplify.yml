version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    preBuild:
      commands:
        - echo "No build required - static site"
    build:
      commands:
        - echo "Skipping build - using pre-built static files"
  artifacts:
    # IMPORTANT - Please verify your artifacts directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
